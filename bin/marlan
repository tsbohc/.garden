#!/usr/bin/env bash

# parse in blocks (separated by an empty line)
#   e.g if all lines begin with - (excluding spaces), use preceeding spaces as list levels
#       if a block begins with $(code), wrap it with code and pre

#code
#    aorste
#    arsotinarst
#    arositnaoresntoaresnt

# this is normal text and $(i this is italic)
